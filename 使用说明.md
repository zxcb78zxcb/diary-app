# 云同步日记本 · 使用说明

手机、电脑打开同一个网址，日记自动双向同步。日记存在**你自己的 GitHub 私有仓库**里，免费、没有第三方服务器。

---

## 一、建一个私有仓库放日记（2 分钟）

1. 打开 [github.com/new](https://github.com/new)
2. Repository name 填 `my-diary`
3. **一定要选 Private（私有）** ← 这是你的日记
4. 勾上 **Add a README file**（仓库不能是完全空的）
5. 点 **Create repository**

## 二、建一个仓库放网页 + 开 Pages（2 分钟）

日记内容和网页要分开放，因为 Pages 网址是公开的。

1. 再新建一个仓库，名字 `diary-app`，这个选 **Public**
2. 进去点 **Add file → Upload files**，把本文件夹里这 5 个文件拖进去：
   - `index.html`
   - `sync-core.js`
   - `sw.js`
   - `manifest.webmanifest`
   - `icon-192.png`、`icon-512.png`
3. 点 **Commit changes**
4. **Settings → Pages**，Branch 选 `main`，点 **Save**
5. 等 1～2 分钟，页面上方出现网址：
   `https://你的用户名.github.io/diary-app/`

> 网页是公开的，但里面没有任何日记内容——别人打开只会看到一个空日记本，要你的 Token 才读得到日记。

## 三、生成 Token（3 分钟）

1. 打开 [github.com/settings/personal-access-tokens/new](https://github.com/settings/personal-access-tokens/new)
2. **Token name**：`diary`
3. **Expiration**：选 `No expiration`（不然到期要重弄）
4. **Repository access**：选 **Only select repositories** → 勾选 `my-diary`（只勾日记那个仓库）
5. **Permissions → Repository permissions** → 找到 **Contents**，右边下拉选 **Read and write**
6. 拉到底点 **Generate token**
7. **把出现的那串 `github_pat_...` 复制下来**——只显示这一次，关掉页面就再也看不到了

## 四、连起来

打开你的 Pages 网址，点右上角 **⚙️**，填：

| 项目 | 填什么 |
|---|---|
| GitHub 用户名 | 你的 GitHub 用户名 |
| 仓库名 | `my-diary`（放日记的那个，不是 diary-app） |
| 访问 Token | 刚才复制的那串 |

点「保存并测试连接」，显示「✓ 连上了」就好了。

手机上打开同一个网址，同样填一次，两边就同步了。

## 五、装到手机主屏（像 App 一样）

- **iPhone（Safari）**：打开网址 → 底部分享按钮 → **添加到主屏幕**
- **Android（Chrome）**：打开网址 → 右上角 ⋮ → **添加到主屏幕**

装完从主屏图标打开，没有浏览器地址栏，断网也能写（联网后自动补同步）。

---

## 日常使用

- 点开就是今天这页，左上角是今天日期
- 边写边自动保存，几秒后自动同步到云端
- 过了零点自动翻到新的一页；同一天重开还是回到今天那页
- 一年一本，第二年自动建新文件夹（仓库里是 `2026/2026-07-29.txt` 这样）
- 点 📚 翻历年日记，点 ⚙️ 里的「导出备份」存一份 json

底部左下角显示同步状态，点一下可以手动立即同步。

## 常见问题

**「找不到这个仓库」**
用户名或仓库名打错了；或者 Token 的 Repository access 没勾上这个仓库。

**「Token 不对或已过期」**
重新生成一个 Token，⚙️ 里重填。

**手机上写的电脑上没看到**
两边都点一下底部的同步状态手动同步一次。同一天在两台设备上都写了，两份内容都会保留，中间用「———— 另一台设备上写的 ————」隔开，不会丢字。

**换手机 / 清了浏览器数据**
日记在 GitHub 上，新设备填一次用户名+仓库名+Token 就全下回来了。

**想直接看日记文件**
去 `my-diary` 仓库，按年份文件夹点进去，都是纯文本 `.txt`。

## 关于 Token 的安全

Token 存在你自己设备的浏览器里，不会上传到任何地方。它的权限被限制成「只能读写 my-diary 这一个仓库的文件」，就算泄露也动不了你的其他仓库。手机丢了的话，去 [github.com/settings/tokens](https://github.com/settings/tokens) 把这个 Token 删掉即可。

## 改了网页之后

如果以后改 `index.html` 或 `sync-core.js`，记得把 `sw.js` 里的 `VERSION = 'diary-v1'` 数字加一，否则手机上会一直用旧的缓存版本。
